# Overview

# The AI API  For Developers

Access hundreds of AI models through a centralized interface.

[![](https://creator.poe.com/img/api/dark.svg)Get an API Key](https://poe.com/api_key)

Python SDKOpenAI PythonNode.jscURL

```
import fastapi_poe as fp
import os

# Initialize with your API key
api_key = os.environ['POE_API_KEY']
message = fp.ProtocolMessage(
    role="user",
    content="Why is the sky blue?"
)

# Stream responses from any model
for partial in fp.get_bot_response_sync(
    messages=[message],
    bot_name="Claude-Sonnet-4",
    api_key=api_key
):
    print(partial)
```

```
import openai
import os

# Drop-in OpenAI replacement
client = openai.OpenAI(
    api_key=os.environ['POE_API_KEY'],
    base_url="https://api.poe.com/v1"
)

response = client.chat.completions.create(
    model="GPT-5",
    messages=[{\
        "role": "user",\
        "content": "Why is the sky blue?"\
    }]
)

print(response.choices[0].message.content)
```

```
import OpenAI from 'openai';

const client = new OpenAI({
    apiKey: process.env.POE_API_KEY,
    baseURL: 'https://api.poe.com/v1'
});

const response = await client.chat
    .completions.create({
        model: 'Grok-4',
        messages: [{\
            role: 'user',\
            content: 'Why is the sky blue?'\
        }]
    });

console.log(response.choices[0]
    .message.content);
```

```
curl https://api.poe.com/v1/chat/completions \
  -H "Authorization: Bearer $POE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Claude-Sonnet-4",
    "messages": [{\
      "role": "user",\
      "content": "Why is the sky blue?"\
    }]
  }'
```

* * *

## [Getting Started](https://creator.poe.com/api-reference/overview\#getting-started)

### [Create Your API Key](https://creator.poe.com/api-reference/overview\#create-your-api-key)

Navigate to [poe.com/api\_key](https://poe.com/api_key) and create your API key.

![Poe API Key Page](https://files.readme.io/2b74562b026bc968fe54d746148458d34537899901fe4b9fb223e956d2a1d68b-Poe_API_Key_2025-04-03_at_4.06.13_PM.jpg)

Keep Your API Key Secure

Your API key provides access to your Poe account and point balance. Never share it publicly, commit it to version control, or expose it in client-side code.

**API Key Best Practices:**

- Store in environment variables, not in code
- Use different keys for development and production
- Rotate keys periodically
- Revoke compromised keys immediately from [poe.com/api\_key](https://poe.com/api_key)

### [Choose Your Integration Method](https://creator.poe.com/api-reference/overview\#choose-your-integration-method)

Pick the method that best fits your project:

Python SDKOpenAI SDKHTTP/REST

**Recommended for Python projects**

Install the official Poe Python SDK:

```
pip install fastapi-poe
```

**Advantages:**

- Native Python interface
- Full feature support (custom parameters, file upload)
- Better error handling
- Async and sync support

[View Python SDK Guide →](https://creator.poe.com/api-reference/client-sdks#option-1-poe-python-library-recommended)

**Best for OpenAI migration**

Use the standard OpenAI SDK with Poe's base URL:

```
pip install openai  # Python
npm install openai  # Node.js
```

**Advantages:**

- Drop-in replacement for OpenAI
- Familiar interface
- Works with existing OpenAI code
- Multi-language support

[View OpenAI SDK Guide →](https://creator.poe.com/api-reference/client-sdks#option-2-openai-compatible-api)

**For any language**

Make direct HTTP requests to the Poe API:

```
curl https://api.poe.com/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"Claude-Sonnet-4","messages":[{"role":"user","content":"Hello!"}]}'
```

**Advantages:**

- Works in any programming language
- No dependencies required
- Maximum flexibility
- Simple REST interface

[View API Reference →](https://creator.poe.com/api-reference/createChatCompletion)

### [Make Your First Request](https://creator.poe.com/api-reference/overview\#make-your-first-request)

Try this quick example to verify your setup:

Python (SDK)Python (OpenAI)cURLNode.js

```
import fastapi_poe as fp
import os

# Use the Poe Python SDK
api_key = os.environ['POE_API_KEY']
message = fp.ProtocolMessage(role="user", content="What are the top 3 things to do in NYC?")

for partial in fp.get_bot_response_sync(
    messages=[message],
    bot_name="Claude-Sonnet-4",
    api_key=api_key
):
    print(partial)
```

```
import openai
import os

# Use the OpenAI SDK with Poe
client = openai.OpenAI(
    api_key=os.environ['POE_API_KEY'],
    base_url="https://api.poe.com/v1"
)

response = client.chat.completions.create(
    model="Claude-Sonnet-4",
    messages=[{"role": "user", "content": "What are the top 3 things to do in NYC?"}]
)

print(response.choices[0].message.content)
```

```
curl https://api.poe.com/v1/chat/completions \
  -H "Authorization: Bearer $POE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Claude-Sonnet-4",
    "messages": [\
      {"role": "user", "content": "What are the top 3 things to do in NYC?"}\
    ]
  }'
```

```
import OpenAI from 'openai';

const client = new OpenAI({
    apiKey: process.env.POE_API_KEY,
    baseURL: 'https://api.poe.com/v1'
});

const response = await client.chat.completions.create({
    model: 'Claude-Sonnet-4',
    messages: [\
        { role: 'user', content: 'What are the top 3 things to do in NYC?' }\
    ]
});

console.log(response.choices[0].message.content);
```

### [Explore the API](https://creator.poe.com/api-reference/overview\#explore-the-api)

Now that you're set up, explore what the API can do:

- **[List Models](https://creator.poe.com/api-reference/listModels)** \- Browse all available models and their capabilities
- **[Chat Completions](https://creator.poe.com/api-reference/createChatCompletion)** \- Learn about advanced features like streaming, tool calling, and multi-modal inputs
- **[Check Balance](https://creator.poe.com/api-reference/getCurrentBalance)** \- Monitor your point balance
- **[Usage History](https://creator.poe.com/api-reference/getPointsHistory)** \- Track your API usage

* * *

## [Quick Links](https://creator.poe.com/api-reference/overview\#quick-links)

### 📚 Client SDKs

Choose between the Poe Python SDK or OpenAI-compatible API

[View SDK Options →](https://creator.poe.com/api-reference/client-sdks)

### 🤖 Available Models

Browse hundreds of AI models from all major providers

[Explore Models →](https://creator.poe.com/api-reference/listModels)

### 💬 Chat Completions

Send messages, stream responses, use tools, and more

[Chat API Reference →](https://creator.poe.com/api-reference/createChatCompletion)

### 💰 Check Balance

Monitor your current point balance

[Balance API →](https://creator.poe.com/api-reference/getCurrentBalance)

### 📊 Usage History

View detailed API usage and spending

[Usage API →](https://creator.poe.com/api-reference/getPointsHistory)

ternal-applications/external-application-guide) for comprehensive tutorials
- **Support**: Contact us at [developers@poe.com](mailto:developers@poe.com)